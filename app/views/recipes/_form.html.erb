<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  ingredients: <%= f.fields_for :ingredients_attributes do |i| %>
    <%= i.label :name %>
    <%= i.text_field :name %>
    <%= i.label :quantity %>
    <%= i.text_field :quantity %>
  <% end %>
  <%= f.submit %>
<% end %>
